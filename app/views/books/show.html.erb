<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
     <title>WebCampBackendNew</title>
  </head>

  <body>
    <%= flash[:notice] %>
    <p>
    <strong>Title:</strong>
    <%= @book.title %>
    </p>

    <p>
    <strong>Body:</strong>
    <%= @book.body %>
    </p>

      <%= link_to "Edit", edit_book_path(@book) %>
      <%= link_to "Back", books_path %>
  </body>
</html>